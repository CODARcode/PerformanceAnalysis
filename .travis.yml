language: cpp
services:
  - docker
before_install:
  - docker build -f docker/ubuntu18.04/openmpi4.0.4/Dockerfile.ad.provdb -t chimbuko/ad:ubuntu18.04-provdb .
script: docker run -it chimbuko/ad:ubuntu18.04-provdb
addons:
  apt:
    packages:
      - docker-ce
